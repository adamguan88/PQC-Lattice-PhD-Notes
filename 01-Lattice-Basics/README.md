
## 1. Definition of a Lattice

Let

$$
B = [b_1,b_2,\ldots,b_n]
$$

be a matrix whose columns are linearly independent vectors.

The lattice generated by $B$ is

$$
\mathcal{L}(B) = \{Bz : z \in \mathbb{Z}^n\}.
$$

Equivalently,

$$
\mathcal{L}(B) = \{z_1b_1 + z_2b_2 + \cdots + z_nb_n : z_i \in \mathbb{Z}\}.
$$

The important point is that the coefficients are integers.

The vectors $b_1,\ldots,b_n$ form a basis of the lattice.




## 2. A Simple Example

Consider the basis vectors

$$
b_1 =
\begin{pmatrix}
1 \\
0
\end{pmatrix},
\qquad
b_2 =
\begin{pmatrix}
0 \\
1
\end{pmatrix}.
$$

The corresponding basis matrix is

$$
B =
\begin{pmatrix}
1 & 0 \\
0 & 1
\end{pmatrix}.
$$

For an integer vector

$$
z =
\begin{pmatrix}
z_1 \\
z_2
\end{pmatrix}
\in \mathbb{Z}^2,
$$

we obtain

$$
Bz =
\begin{pmatrix}
z_1 \\
z_2
\end{pmatrix}.
$$

Therefore,

$$
\mathcal{L}(B) = \mathbb{Z}^2.
$$

In this simple example, the lattice is the ordinary two-dimensional integer grid.




## 3. Integer Combinations

The key idea behind a lattice is the use of integer combinations.

For two basis vectors $b_1$ and $b_2$, every lattice point has the form

$$
z_1b_1 + z_2b_2,
$$

where

$$
z_1,z_2 \in \mathbb{Z}.
$$

For example, choosing

$$
z_1 = 2,
\qquad
z_2 = -1,
$$

gives the lattice vector

$$
2b_1 - b_2.
$$

More generally,

$$
\mathcal{L}(B) = \{Bz : z \in \mathbb{Z}^n\}.
$$

The restriction

$$
z \in \mathbb{Z}^n
$$

is what makes the set discrete.

If we allowed arbitrary real coefficients instead,

$$
z \in \mathbb{R}^n,
$$

we would generally obtain a continuous vector space rather than a discrete lattice.


## 4. Norms and Length

To talk about short lattice vectors, we need a way to measure the length of a vector.

For a vector

$$
x = (x_1,x_2,\ldots,x_n),
$$

the Euclidean norm, or $l_2$ norm, is defined as

$$
\|x\|_2 = \sqrt{x_1^2+x_2^2+\cdots+x_n^2}.
$$

For example, consider

$$
x = (3,4).
$$

Its Euclidean norm is

$$
\|x\|_2 = \sqrt{3^2+4^2} = 5.
$$

Therefore, the norm gives us a numerical measure of the length of a vector.



## 5. Why Do We Care About Short Vectors?

Suppose we have a lattice

$$
\mathcal{L}(B) = \{Bz : z \in \mathbb{Z}^n\}.
$$

There are usually infinitely many lattice vectors.

Some are very long, while others are relatively short.

For example, if

$$
v \in \mathcal{L}(B),
$$

then

$$
2v \in \mathcal{L}(B)
$$

as well.

But

$$
\|2v\|_2 = 2\|v\|_2.
$$

So if we want to understand the geometry of a lattice, naturally we want to know how short its nonzero vectors can be.

This leads to one of the central computational problems in lattice theory.



## 6. The Shortest Vector Problem

The **Shortest Vector Problem**, usually abbreviated as **SVP**, asks us to find a shortest nonzero vector in a lattice.

Define the first minimum of the lattice as

$$
\lambda_1(\mathcal{L}) = \min_{v \in \mathcal{L}\setminus\{0\}} \|v\|_2.
$$

A shortest vector is a vector

$$
v^\star \in \mathcal{L}\setminus\{0\}
$$

such that

$$
\|v^\star\|_2 = \lambda_1(\mathcal{L}).
$$

In simple language:

**SVP asks:**

> Given a lattice, can we find its shortest nonzero vector?



## 7. A Simple SVP Example

Consider the lattice

$$
\mathcal{L} = \mathbb{Z}^2.
$$

The vector

$$
v = (1,0)
$$

belongs to the lattice.

Its length is

$$
\|v\|_2 = 1.
$$

No nonzero integer vector can have Euclidean norm smaller than $1$.

Therefore,

$$
\lambda_1(\mathbb{Z}^2) = 1.
$$

For this particular lattice, SVP is trivial.

The interesting situation is high-dimensional lattices with complicated bases.



## 8. The Important Distinction

A lattice can have a very simple basis while the same lattice can also be represented using a very complicated basis.

For example, two different basis matrices may generate the same lattice:

$$
\mathcal{L}(B) = \mathcal{L}(B').
$$

However, the vectors appearing in $B$ and $B'$ may have very different lengths.

This observation is fundamental.

A lattice basis is not necessarily a collection of short vectors.

Finding a good basis is therefore itself an important algorithmic problem.

This will eventually lead us to lattice reduction algorithms such as **LLL** and **BKZ**.




## 9. The Closest Vector Problem

The **Closest Vector Problem**, or **CVP**, asks a different question from SVP.

Instead of looking for the shortest nonzero lattice vector, we are given a target point and asked to find the lattice point closest to it.

Let

$$
t \in \mathbb{R}^n
$$

be a target vector.

We want to find a lattice vector

$$
v^\star \in \mathcal{L}
$$

that minimizes the distance to $t$.

Formally,

$$
v^\star = \arg\min_{v\in\mathcal{L}} \|t-v\|_2.
$$


In simple language:

**CVP asks:**

> Given a target point, which lattice point is closest to it?



## 10. SVP vs. CVP

The difference between SVP and CVP can be summarized as follows.

SVP asks for the shortest nonzero lattice vector:

$$
v^\star = \arg\min_{v\in\mathcal{L}\setminus\{0\}} \|v\|_2.
$$

CVP asks for the lattice vector closest to a given target:

$$
v^\star = \arg\min_{v\in\mathcal{L}} \|t-v\|_2.
$$

Therefore:

$$
\text{SVP} \rightarrow \text{find a short vector}
$$

while

$$
\text{CVP} \rightarrow \text{find a close vector}.
$$

The two problems are closely related, but they are not the same computational problem.



## 11. A Simple CVP Example

Consider the integer lattice

$$
\mathcal{L} = \mathbb{Z}^2.
$$

Suppose the target is

$$
t = (2.2,1.7).
$$

Some nearby lattice points are

$$
(2,2),\quad (2,1),\quad (3,2),\quad (3,1).
$$

The distance from $t$ to $(2,2)$ is

$$
\mathrm{dist}(t,(2,2))
=
\sqrt{(2.2-2)^2+(1.7-2)^2}
$$

Therefore,

$$
\mathrm{dist}(t,(2,2))
=
\sqrt{0.2^2+(-0.3)^2}
$$

So,

$$
\mathrm{dist}(t,(2,2))
=
\sqrt{0.13}
$$

The CVP problem asks us to determine which lattice point minimizes this distance.



## 12. Why SVP and CVP Matter

SVP and CVP are fundamental problems in the study of computational lattices.

They also help us understand why lattice-based cryptography is interesting.

At a high level, lattice cryptography relies on computational problems that are believed to remain difficult even for quantum computers.

However, practical cryptographic assumptions such as LWE and SIS are not simply identical to exact SVP or CVP.

Instead, there are mathematical reductions connecting certain lattice problems to these cryptographic problems.

This distinction will become increasingly important.



## 13. From Geometry to Cryptography

We can now summarize the first stage of the course:

$$
\text{Lattice}
\rightarrow
\text{Basis}
\rightarrow
\text{Norm}
\rightarrow
\text{Short Vectors}
\rightarrow
\text{SVP/CVP}.
$$

The next step is to transform these geometric ideas into computational assumptions that can actually be used to construct cryptographic systems.

Two particularly important problems are:

$$
\text{LWE}
$$

and

$$
\text{SIS}.
$$

These problems will be the focus of the next lectures.


## 14. The Main Idea to Remember

A lattice is generated by integer combinations of basis vectors:

$$
\mathcal{L}(B) = \{Bz : z \in \mathbb{Z}^n\}.
$$

We measure vector length using a norm such as

$$
\|x\|_2 = \sqrt{x_1^2+\cdots+x_n^2}.
$$

SVP asks for the shortest nonzero lattice vector:

$$
\lambda_1(\mathcal{L})
=
\min_{v\in\mathcal{L}\setminus\{0\}}
\|v\|_2.
$$

CVP asks for the lattice vector closest to a target:

$$
v^\star
=
\operatorname*{arg\,min}_{v\in\mathcal{L}}
\|t-v\|_2.
$$

These geometric problems provide the mathematical background for understanding modern lattice-based cryptography.



## 15. Next Lecture

The next lecture begins with a surprisingly simple-looking equation:

$$
b = As + e \pmod q.
$$

This is the basic form of an LWE sample.

We will ask why adding a small error vector $e$ can transform an apparently ordinary linear equation into a computationally difficult problem.

The next lecture will therefore move from:

$$
\text{geometry}
$$

to

$$
\text{computational hardness}.
$$